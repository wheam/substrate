# bootstrap — minimal 示例的上手协议

> 本示例从略，完整协议见 Substrate 引擎 `template/governance/bootstrap.md`。要点：

1. `git pull` 同步。
2. 读 `CONSTITUTION.md` + `zones.md`。
3. 配本地身份（由本机 runtime 的 adapter 决定；本地清单不入库）。
4. 装适用 skill（`substrate-sync`）。
5. 要写某个 zone 前，先读该 zone README 顶部的 **Agent Packet**，再动手。
