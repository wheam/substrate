# raw — 原始素材存档（只读）

> **Agent Packet**
> - zone: raw
> - 维护 skill: `substrate-curator`（只追加，不改）
> - canonical: 本目录下的原始素材（`disposition: reference`）
> - 写前查: 是否已归档过同一素材
> - 写后更新: 本 README 索引；把**正文/更正写进对应知识页**，不改原始素材
> - doctor 检查: 大二进制是否误入（应走 admission：本体不进库，只存引用 + 文字代理）

## 规则

- **只读**：原始素材只追加归档，**不修改**。要更正/提炼，写进 `../knowledge/` 的页，并 `[[链接]]` 回来。
- **不是云盘**：多媒体/大文件按 admission 走 **Reference**——只放引用 + 元数据 + 文字代理（转写/OCR/描述），本体不进库；小图可 git-lfs。

## 索引

| 素材 | 来源 | 关联知识页 |
|------|------|-----------|
| _（空）_ | | |
